name: CI - Compose & Newman

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  compose-test:
    runs-on: ubuntu-latest
    services: {}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Debug runner + Docker versions
        run: |
          uname -a || true
          docker --version || true
          docker buildx version || true

      - name: Install Docker Compose plugin (apt or fallback download)
        run: |
          set -e
          sudo apt-get update || true
          if sudo apt-get install -y docker-compose-plugin; then
            docker compose version
          else
            echo "apt install failed — falling back to downloading docker-compose binary"
            sudo mkdir -p /usr/local/lib/docker/cli-plugins
            sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-linux-x86_64" -o /usr/local/lib/docker/cli-plugins/docker-compose
            sudo chmod +x /usr/local/lib/docker/cli-plugins/docker-compose
            docker compose version
          fi

      - name: Build and start compose stack
        run: |
          set -euxo pipefail
          # ensure .env exists to avoid "env file ... .env not found" errors on runner
          if [ ! -f .env ]; then
            echo "# generated by CI" > .env
          fi
          # ensure external network used by compose exists on runner
          if ! docker network inspect class-net >/dev/null 2>&1; then
            docker network create class-net || true
          fi
          docker compose up -d --build
          echo "Waiting for API health endpoint..."
          for i in $(seq 1 20); do
            if docker compose exec -T api sh -c 'python -c "import urllib.request; urllib.request.urlopen(\"http://127.0.0.1:8000/health\", timeout=3).read()"' >/dev/null 2>&1; then
              echo "API is ready"
              break
            fi
            echo "Waiting for API startup ($i/20)..."
            sleep 5
          done
          docker compose ps

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Run Newman tests
        run: |
          set -euxo pipefail
          mkdir -p reports
          npm ci
          ls -la postman/collections postman/environments reports
          npm run test:compose -- --reporter-json-export reports/newman-ci.json
          echo "Newman exit status: $?"

      - name: Tear down Compose
        if: always()
        run: |
          echo "workspace files before teardown:" && ls -la
          if [ ! -f .env ]; then
            echo "creating .env for teardown" > .env
          fi
          # ignore errors during teardown to avoid failing the job when compose artifacts are missing
          docker compose down -v || true
